# Plugins do MkDocs usados por este site.
# A base (mkdocs, material, pymdown-extensions) ja vem na imagem oficial.

# Mostra no rodape de cada pagina quando ela foi atualizada, lendo do git.
mkdocs-git-revision-date-localized-plugin==1.2.9
