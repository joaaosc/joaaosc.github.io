# Hillink

Site estático feito com mkdocs para exposição de projeto pessoal temporário.
Usa justfile.

## Editar

```bash
just dev        # http://localhost:8080
just imagem     # gera imagem nova
```

Páginas em `docs/*.md`. Navegação em `mkdocs.yml`, seção `nav`.


